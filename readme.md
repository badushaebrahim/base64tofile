 npm install express body-parser sharp mime-types xml2js  json-to-graphql jszip archiver 
 npm install jsonschema2pojo
 const { promisify } = require('util');
const { spawn } = require('child_process');
